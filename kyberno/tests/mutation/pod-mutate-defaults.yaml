apiVersion: v1
kind: Pod
metadata:
  name: mutate-defaults
  namespace: default
spec:
  restartPolicy: Never
  containers:
    - name: c
      image: busybox:1.36
      command: ["sh", "-c", "id && sleep 5"]
